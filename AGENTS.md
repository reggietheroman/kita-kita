# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Philippine DPA (RA 10173) docs, rules, and skills are maintained in the sibling project `npc-dpa-compliance`. Local copies under `docs/`, `.cursor/`, and `scripts/verify-privacy-gates.js` are the installed kit for this app.
